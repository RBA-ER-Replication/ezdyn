% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alt_paths.R
\name{get_alt_paths}
\alias{get_alt_paths}
\title{Calculate alternative policy paths for multiple models.}
\usage{
get_alt_paths(
  alt_paths,
  models,
  baseline,
  use_cd = FALSE,
  lambda = 0,
  instrument_display_name = "Cash Rate",
  instrument_shock_name = "Monetary policy shock",
  forecast_start,
  forecast_end,
  data_start
)
}
\arguments{
\item{alt_paths}{Numeric vector or data frame of named alternative policy paths.}

\item{models}{Named list of full model MOO pairs.}

\item{baseline}{Canonical result of [import_baseline()].}

\item{use_cd}{Whether to use cognitive discounting where supported.}

\item{lambda}{Cognitive discounting parameter in $[0, 1]$.}

\item{instrument_display_name}{Instrument variable display or Dynare name.}

\item{instrument_shock_name}{Instrument shock description or code.}

\item{forecast_start}{First forecast quarter.}

\item{forecast_end}{Last forecast quarter.}

\item{data_start}{First baseline quarter returned.}
}
\value{
Pretty tibble containing the shared baseline and model-specific paths.
}
\description{
Joins a canonical [import_baseline()] result to each named alternative policy
path, solves targeted IRFs for every requested model, and returns level paths.
The returned baseline is shared and has missing `model_name` and `dynare_name`.
If a model cannot supply anticipated shocks, cognitive discounting falls back
to ordinary targeted IRFs with a warning and `effective_use_cd = FALSE`.
}
