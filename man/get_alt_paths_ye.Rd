% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alt_paths.R
\name{get_alt_paths_ye}
\alias{get_alt_paths_ye}
\title{Convert selected quarterly alternative paths to year-ended rates.}
\usage{
get_alt_paths_ye(pretty_df, output_ye_vars = NA, forecast_start)
}
\arguments{
\item{pretty_df}{Output pretty data frame from [get_alt_paths()].}

\item{output_ye_vars}{Optional character vector of variable names in
`dynare_name` format to transform. If `NA` (default), all variables are
transformed.}

\item{forecast_start}{First forecast date. It is validated for compatibility
with the alternative-path API.}
}
\value{
A pretty data frame with the same rows and columns as `pretty_df`.
  Selected rows have year-ended values, `display_unit = "Year-ended"`, and a
  ` (YE)` display-name suffix. Initial rows without four quarters remain `NA`.
}
\description{
Transforms only the requested alternative `dynare_name` values. Their shared
Baseline rows are transformed by `display_name`; all other rows are retained
unchanged. Values are calculated only where four consecutive quarters exist.
}
