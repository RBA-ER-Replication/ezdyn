% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_tables.R
\name{get_param_table}
\alias{get_param_table}
\title{Build a parameter table from a model object.}
\usage{
get_param_table(
  M_,
  type = NULL,
  mode = c("katex", "dt"),
  dp = 3,
  as_of = NULL,
  show_codes = FALSE
)
}
\arguments{
\item{M_}{Model object containing `param_table` (populated by
[read_dynare()]/[read_moo()]; not currently populated by [custom_moo()]).}

\item{type}{Optional value to filter by `Type` column.}

\item{mode}{Table format: `"katex"` (default) returns a static, styled
`kableExtra` HTML table (KaTeX-rendered symbols, optional footnote;
suited to reports/Rmd); `"dt"` returns an interactive, sortable/
searchable [DT::datatable()] (suited to a live Shiny dashboard).}

\item{dp}{Number of decimal places for rounding numeric values.}

\item{as_of}{Optional timestamp text appended as a table footnote
(`mode = "katex"` only).}

\item{show_codes}{Whether to include the model's own raw parameter code
(`Dyn_Symbol`) alongside the rendered symbol - intended for advanced
users who want to match displayed parameters back to the underlying
model code.}
}
\value{
HTML `kableExtra` table (`mode = "katex"`) or a `DT::datatable`
  htmlwidget (`mode = "dt"`).
}
\description{
Build a parameter table from a model object.
}
