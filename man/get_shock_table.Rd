% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary_tables.R
\name{get_shock_table}
\alias{get_shock_table}
\title{Build a shock table from a model object.}
\usage{
get_shock_table(M_, mode = c("katex", "dt"), show_codes = FALSE)
}
\arguments{
\item{M_}{Model object containing `shock_meta` (populated by
[read_dynare()]/[custom_moo()] when a metadata workbook's optional
`shocks` sheet is supplied).}

\item{mode}{Table format: `"katex"` (default) returns a static, styled
`kableExtra` HTML table; `"dt"` returns an interactive, sortable/
searchable [DT::datatable()].}

\item{show_codes}{Whether to include the model's own raw shock code
(`Code`) alongside the description - intended for advanced users.}
}
\value{
HTML `kableExtra` table (`mode = "katex"`) or a `DT::datatable`
  htmlwidget (`mode = "dt"`).
}
\description{
Lists the descriptions a model's metadata defines for its shocks,
analogous to [get_param_table()] for parameters. Shocks without a defined
description fall back to showing their model code as `Shock`.
}
