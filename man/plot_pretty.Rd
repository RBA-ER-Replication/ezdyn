% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretty_plot.R
\name{plot_pretty}
\alias{plot_pretty}
\alias{plot_pretty_irf}
\title{Plot pretty IRF or alternative-path output.}
\usage{
plot_pretty(
  df,
  display_names = NULL,
  dynare_names = NULL,
  shocks = NULL,
  line_style = c("combined", "split"),
  plotter = c("ggrba", "ggplot"),
  title = NULL,
  cumulate = FALSE,
  baseline_name = "Baseline",
  baseline_colour = "royalblue",
  collapse_baseline = TRUE
)

plot_pretty_irf(
  df,
  dynare_names = NULL,
  display_names = NULL,
  shocks = NULL,
  line_style = c("combined", "split"),
  plotter = c("ggrba", "ggplot"),
  title = NULL,
  cumulate = FALSE,
  baseline_name = "Baseline",
  baseline_colour = "royalblue",
  collapse_baseline = TRUE
)
}
\arguments{
\item{df}{Data frame created by [pretty_irf()] or [get_alt_paths()].}

\item{display_names}{Optional vector of display names to include.}

\item{dynare_names}{Optional vector of Dynare names to include.
For alternative paths, a requested Dynare name is first mapped to its
display name, which keeps the shared Baseline and matching responses from
all models. Prefer `display_names` for unambiguous multi-model selection:
the same Dynare name can identify different variables across models.}

\item{shocks}{Optional vector of shocks to include (default all).}

\item{line_style}{"combined" uses a single legend for shock-model pairs; "split"
uses colour for shock and linetype for model.}

\item{plotter}{Plotting backend. `"ggrba"` (default) or `"ggplot"`.}

\item{title}{Provide a custom title for the chart.}

\item{cumulate}{Cumulate the response variables. Can be FALSE (default), TRUE, or a
string vector for which display names the responses should be cumulated for. TODO:
Not currently implemented.}

\item{baseline_name}{Alternative-path label that identifies the shared baseline.}

\item{baseline_colour}{Colour used for the shared alternative-path baseline.}

\item{collapse_baseline}{Whether to collapse identical manually duplicated
alternative-path baseline rows. Non-identical duplicates always error.}
}
\value{
List with `graph`, `graph_data`, and `graph_fname`.
}
\description{
Plot pretty IRF or alternative-path output.
}
\details{
For each `display_name`, the plotting pipeline requires one unique non-empty
value for `units`/`display_unit`. IRFs use `unit_symbol_irf`; alternative
paths use `unit_symbol_baseline` because their values are levels.
Alternative-path data are recognised by their `date` and `path_name` columns.
They use `date` on the x-axis and do not apply `shocks` filtering.
}
