% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretty_plot.R
\name{ezdyn_resolve_plotter}
\alias{ezdyn_resolve_plotter}
\title{Resolve the plotting backend, falling back to ggplot2 when ggrba is unavailable.}
\usage{
ezdyn_resolve_plotter(
  plotter = NULL,
  ggrba_available = requireNamespace("ggrba", quietly = TRUE)
)
}
\arguments{
\item{plotter}{`NULL`, `"ggrba"`, or `"ggplot"`.}

\item{ggrba_available}{Whether the optional ggrba package is installed.
Exposed as an argument so callers (including tests) can inject
availability without depending on the real installed-package state.}
}
\value{
`"ggrba"` or `"ggplot"`.
}
\description{
`NULL` (the public plotting functions' default) auto-selects `"ggrba"` when
the optional ggrba package is installed, otherwise falls back to
`"ggplot"`. An explicit `"ggrba"` request still errors when the package is
not installed, since that is an explicit user choice rather than a default.
}
\keyword{internal}
