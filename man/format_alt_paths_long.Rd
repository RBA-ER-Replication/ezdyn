% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alt_paths.R
\name{format_alt_paths_long}
\alias{format_alt_paths_long}
\title{Prepare alternative paths output as a wide table}
\usage{
format_alt_paths_long(
  pretty_df,
  output_vars = NA,
  baseline_name_value = NA,
  forecasts_start_date,
  output_start_date,
  output_end_date,
  include_baseline_path = FALSE
)
}
\arguments{
\item{pretty_df}{Output pretty data frame from [get_alt_paths()]. A
year-ended variable (e.g. `infl_obs_ye`) is an ordinary metadata-declared
derived variable, resolved automatically like any other response.}

\item{output_vars}{Optional vector or list of variable names in
`dynare_name` format to keep in the output. If `NA` (default), all
variables are included.}

\item{baseline_name_value}{Scalar baseline label to include in the export.}

\item{forecasts_start_date}{First forecast date to include for alternative paths.}

\item{output_start_date}{First baseline-history date to include.}

\item{output_end_date}{Last date to include in the export.}

\item{include_baseline_path}{Whether to include a separate shared Baseline
row covering the output range.}
}
\value{
A data frame with `Model`, `Mnemonic`, `Variable Name`, `Baseline Name`,
  `Path Name`, and one column per quarter containing the corresponding values.
  When `include_baseline_path` is `TRUE`, this includes a shared Baseline row
  for each selected response.
}
\description{
Prepare alternative paths output as a wide table
}
