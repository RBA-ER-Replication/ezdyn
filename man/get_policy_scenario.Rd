% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy_scenarios.R
\name{get_policy_scenario}
\alias{get_policy_scenario}
\title{Run one optimal-policy scenario for one model.}
\usage{
get_policy_scenario(
  policy_strategy,
  baseline,
  model,
  use_cd = FALSE,
  lambda = 0,
  forecast_start,
  forecast_end,
  loss_variable_vintages = NULL,
  preloss_start = NULL,
  policy_irfs = NULL
)
}
\arguments{
\item{policy_strategy}{Named list with `output_name`, `output_variables`,
`loss_variables`, `loss_weights`, `discount_factor`, `T_loss`,
`T_instrument`, and `commit` (`"commit"`, `"timeless"`, or `"disc"`).
Optional `instrument_variable` and `instrument_shock` identify the model's
policy instrument and innovation. Variable entries (`output_variables`,
`loss_variables`, `instrument_variable`, and constraint `variable`s) may
be display names or dynare codes, resolved against `model`; a trailing
`_gap_loss` suffix is resolved on the underlying variable.
`instrument_shock` may be a shock description or code.}

\item{baseline}{Wide baseline data frame keyed by `date` plus one column per
variable referenced from `policy_strategy`, named by either `model`'s own
dynare code or its display name.}

\item{model}{Full Dynare or custom-MOO pair.}

\item{use_cd}{Whether to use partially anticipated policy shocks.}

\item{lambda}{Cognitive-discounting parameter.}

\item{forecast_start}{First forecast quarter.}

\item{forecast_end}{Final forecast quarter.}

\item{loss_variable_vintages}{Optional long history for timeless commitment.}

\item{preloss_start}{First historical commitment quarter for timeless policy.}

\item{policy_irfs}{Optional cached array returned by [build_policy_irfs()].}
}
\value{
A long data frame containing baseline and alternative paths.
}
\description{
`baseline` has a quarterly `date` column and includes the output, loss,
constraint, and instrument variables named in `policy_strategy`, each
column keyed by either `model`'s own dynare code or (as in a canonical
baseline returned by [import_baseline()]) its display name. The returned
paths are rescaled and labelled with model metadata.
}
